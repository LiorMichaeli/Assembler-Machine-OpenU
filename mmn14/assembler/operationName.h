#ifndef OPERATION_NAME_H_    /* #include guards */
#define OPERATION_NAME_H_

typedef enum {mov, cmp, add, sub, not, clr, lea, inc, dec, jmp, bne, red, prn, jsr, rts, stop, illegalOperation} operationName;

#endif
